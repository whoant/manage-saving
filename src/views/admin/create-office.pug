extends ../layout/main.pug
block navbar
    include ../layout/navbarAdmin.pug
block content
    .row
        .col-xl-12.col.md-6.col-12
            .card.mt-5
                .card-body
                    h3.card-title.text-center Thêm chức vụ mới
                    .row
                        .col-12

                            form(action="/admin/office/create", method="post")
                                .form-group
                                    label(for="name") Tên
                                    input#name.form-control(type="text", name="name", value= values ? values.name : "")
                                button.btn.btn-danger Tạo chức vụ mới
